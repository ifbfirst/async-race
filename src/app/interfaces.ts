export interface CarInterface {
  name: string;
  color: string;
  id: string;
}

export interface Engine {
  velocity: number;
  distance: number;
}

export interface WinnerInterface {
  id: number;
  wins: number;
  time: number;
}
